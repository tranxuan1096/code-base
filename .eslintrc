{
  "extends": ["react-app"],
  "overrides": [
    {
      "files": ["**/*.ts?(x)", "**/*.js?(x)"],
      "rules": {
        "no-console": ["warn", { "allow": ["error", "info"] }]
      }
    }
  ]
}
